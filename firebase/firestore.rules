rules_version = '2';

// Read-only public glossary for v1. Writes via Admin SDK / console only.
service cloud.firestore {
  match /databases/{database}/documents {
    match /glossary_entries/{entryId} {
      allow read: if true;
      allow write: if false;
    }
  }
}
