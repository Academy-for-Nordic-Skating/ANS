---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# ANS Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-03

## Active Technologies

- Dart (SDK version pinned by Flutter); Flutter stable channel (exact version locked in `pubspec`/CI) + Flutter SDK; `firebase_core`, `cloud_firestore` (optional cache/offline), `firebase_storage` (if resolving refs client-side), `cloud_functions` or `http` client for Functions HTTP API; `flutter/services` (`Clipboard`); PWA: `flutter build web` with web manifest + service worker (001-pwa-term-glossary)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Dart (SDK version pinned by Flutter); Flutter stable channel (exact version locked in `pubspec`/CI)

## Code Style

Dart (SDK version pinned by Flutter); Flutter stable channel (exact version locked in `pubspec`/CI): Follow standard conventions

## Recent Changes

- 001-pwa-term-glossary: Added Dart (SDK version pinned by Flutter); Flutter stable channel (exact version locked in `pubspec`/CI) + Flutter SDK; `firebase_core`, `cloud_firestore` (optional cache/offline), `firebase_storage` (if resolving refs client-side), `cloud_functions` or `http` client for Functions HTTP API; `flutter/services` (`Clipboard`); PWA: `flutter build web` with web manifest + service worker

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
